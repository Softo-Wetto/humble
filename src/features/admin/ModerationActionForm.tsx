"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
export function ModerationActionForm({userId}:{userId:string}){const router=useRouter();const[pending,setPending]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setPending(true);const form=new FormData(event.currentTarget);await fetch(`/api/admin/users/${userId}/action`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(form.entries()))});setPending(false);router.refresh();}return <form className="moderation-form" onSubmit={submit}><label>Action<select name="action"><option value="suspend">Suspend account</option><option value="unsuspend">Restore account</option><option value="hide_profile">Hide profile</option><option value="restore_profile">Restore profile</option><option value="close_account">Close account</option><option value="correct_birth_date">Correct birth date</option></select></label><label>Reason<textarea name="reason" rows={3} required /></label><label>Corrected birth date <small>only used for correction</small><input name="birthDate" type="date" /></label><button className="button" disabled={pending}>{pending?"Recording...":"Apply and record action"}</button></form>;}
